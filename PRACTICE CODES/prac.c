#include<stdio.h>
int main() {
    double number;
    char alphabet;

    printf("enter the number: ");
    scanf("%lf", & number);
    printf("the number is = %lf", number);

    printf("enter the alphabet: ");
    scanf("\n%c", & alphabet);

    //printf("the number is = %lf", number);
    printf("\nthe alphabet is= %c ", alphabet);


return 0;

}
